Album FIFA World Cup 2022
Este es año importante para el futbol profesional, se juega la Copa del Mundo 2022, y la editorial Panene lo sabe, por lo cual ha lanzado un álbum con los equipos y jugadores que jugarán este magnánimo evento deportivo. 
Para facilitar el intercambio y llenado de los álbumes la editorial ha decidido lanzar una aplicación que permita llevar un control sobre el llenado de los álbumes, donde cada persona siempre tiene una lista con los números de las láminas (calcomanías, fichas o como le llamen en cada región) que les faltan para completar el álbum, y otra lista con los números de las láminas que están dispuestos a intercambiar. 
Adicionalmente, se nota que las láminas pueden ser categorizadas por equipo (Alemania, Brasil, Japón, etc.) y han decido numerar éstas categorías como 1, 2, 3, 4, 5, .., etc.

Usted hace parte del equipo de desarrollo encargado por Panene para crear esta aplicación; el software tiene muchas funciones y usted va a trabajar como desarrollador en los módulos iniciales. 
El arquitecto de Software, le indica a usted, los primeros requerimientos para construir una clase de Java que hace parte del módulo más importante.  El arquitecto le pide que esta clase se llame Album.
1). (Vale 1) El arquitecto solicita que exista un método llamado "clases" que, dada una lista de enteros, donde cada entero es la categoría de la lámina, retorne una lista con las categorías únicas.
Por ejemplo, suponiendo que 10 láminas son categorizadas de la siguiente manera:
[1,2,5,5,5,1,2,5,5,5]
la función debe retornar la lista
 [1,2,5]
2).  (Vale 1) El arquitecto solicita que exista un método llamado "meFaltanDeLaClase"  el cual recibe tres parámetros:
1.	El primero es una lista con las láminas que a una persona le hacen falta para completar el álbum. 
2.	El segundo es una lista de las categorías a la que pertenece cada una de las láminas. 
3.	El tercero es un entero representando una de las categorías. 
Este método debe retornar una lista con los números de la lámina que faltan para completar pero que son de la categoría especificada en el tercer parámetro.
Por ejemplo, si se ejecuta la función:
meFaltanDeLaClase([1,3,6,8], [1,2,5,5,5,1,2,5,5,5], 5)
debe retornar la lista
[3,8]
Por ejemplo, si ejecutan la
función:
meFaltanDeLaClase([1,3,6,8], [1,2,5,5,5,1,2,5,5,5], 2)
 Se debe retornar la lista:
[1,6]
 3). (Vale 1) El arquitecto solicita que exista un metodo llamado "noTengo" que dada una lista con las láminas que tiene un usuario A, una lista con las láminas que tiene un usuario B, retorne la lista con las láminas que son de interés para el usuario B (Las que tiene A que no tiene B).
Por ejemplo si se llama:
notengo([3,5,7,10,15,16],[4,10,5,8])
Se debe retornar la lista:
[3,7,15,16]
4.  (Vale 1). Es normal que, al comprar sobres con láminas, muchas de estas salgan repetidas, por lo cual se necesita hacer intercambios de láminas y así mismo facilitar el proceso para completar el álbum. El arquitecto solicita que exista un metodo llamado "puedoCambiar" que reciba la lista de duplicados que tiene el usuario A y la lista de duplicados que tiene el usuario B y que retorne el número (cantidad) de láminas que pueden cambiar.
Por ejemplo, llamar la función:
puedocambiar([3,5,7,10,15,16],[4,10,5,8,26])
Debe retornar
2
Porque al usuario B le interesan las láminas [3,7,15,16] del usuario A, mientras que al usuario A solo le interesan las láminas [4,8] del usuario B. Para que sea evaluada correctamente, la función debe retornar un Integer.
(IMPORTANTE) Entrada:
Este programa no requiere entrada. Ni generará salida. Se requiere que el estudiante genere un archivo con el nombre Album.java sin main y que se respeten los nombres de las funciones dadas y sus parámetros.

